APP_NAME="Forti API"
ENVIRONMENT=development

# Local SQLite (desarrollo):
DATABASE_URL=sqlite:///./forti.db

# Vercel Postgres provee POSTGRES_URL automáticamente.
# En local puedes copiar el valor de POSTGRES_URL aquí si querés usar la DB remota.

APP_PASSWORD=forti2026
SECRET_KEY=change-me-in-production

# URL del frontend en producción (para CORS):
CORS_EXTRA_ORIGINS=https://forti-demo.vercel.app
